# Its own workspace root, so this repository builds on its own.
[workspace]

[package]
name = "xmip-core-logic"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
repository = "https://github.com/IlleNilsson/xmip-core-logic"
description = "Common contracts for method and operation semantics layered over transport and message representation."

[lib]
path = "src/lib.rs"

[lints.rust]
unsafe_code = "forbid"
